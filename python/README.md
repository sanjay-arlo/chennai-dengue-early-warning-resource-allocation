# Optional Python Layer

Baseline workflow: **Excel → SQL → Power BI → Decision**.

Use Pandas + NumPy for EDA, rolling-trend checks, anomaly detection, signal relationships and repeatable transformation when that adds value.

Suggested use here:
- Pandas: weekly ward surveillance profiling and intervention-gap analysis.
- NumPy: rolling statistics, percentile thresholds and numerical sensitivity checks.

Python remains an optional accelerator; governed KPI logic stays in SQL and Power BI.
